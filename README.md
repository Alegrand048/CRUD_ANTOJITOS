# CRUD_ANTOJITOS

## Como abrir el proyecto
Debemos abrir una terminal y escribir el siguiente comando:
npm run dev:all
Esto es para que se inicie el servidor y la base de datos (API) al mismo tiempo, ya que el proyecto esta configurado para que ambos se inicien con este comando
